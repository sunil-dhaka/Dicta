package com.example.dicta.presentation.theme

import androidx.compose.ui.graphics.Color

// Neutrals — warm paper on the light side, cool ink on the dark side.
val Paper = Color(0xFFFAFAF7)
val PaperSurface = Color(0xFFFFFFFF)
val Ink = Color(0xFF18181B)
val InkMuted = Color(0xFF52525B)
val InkSubtle = Color(0xFF71717A)
val Hairline = Color(0xFFE4E4E7)

val InkDark = Color(0xFF0A0A0B)
val InkDarkSurface = Color(0xFF18181B)
val Parchment = Color(0xFFFAFAFA)
val ParchmentMuted = Color(0xFFA1A1AA)
val ParchmentSubtle = Color(0xFF71717A)
val HairlineDark = Color(0xFF27272A)

// Ember — used only when live/recording. Everything else stays monochrome.
val Ember = Color(0xFFDC2626)
val EmberSoft = Color(0xFFFEE2E2)
val EmberDark = Color(0xFFEF4444)
val EmberSoftDark = Color(0xFF450A0A)

// Legacy names kept for any screen that still imports them.
val RecordingRed = Ember
val RecordingRedDark = EmberDark
